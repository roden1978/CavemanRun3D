using System;
using UnityEngine;
using Random = UnityEngine.Random;

namespace HalfDiggers.Runner
{
    public class PillarLampView : MonoBehaviour
    {
        /*
        [SerializeField] private PlatformView _platform;
        [Range(0, 1)] [SerializeField]
        private float _chanceOfStaying = 0.5f;

        private void Start()
        {
            _platform.Activated += ChanceOfActive;
        }

        private void OnDestroy()
        {
            _platform.Activated -= ChanceOfActive;
        }
        
        private void ChanceOfActive()
        {
            gameObject.SetActive(true);
            
            if(Random.value > _chanceOfStaying) gameObject.SetActive(false);
        }
        */
        
        
    }
}